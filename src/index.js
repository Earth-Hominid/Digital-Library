import data from "./data.js";




// Create a function that iterates through the library and displays the book information on the page. Book are to be displayed on a new card. 
const displayLibrary = () => {
  for(let element of library) {
    return {};
}
}


displayLibrary();


deletButton.addEventListener('click',  )


// Create a function to add a book to the library.